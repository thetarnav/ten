{
    "name": "ten",
    "type": "module",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "test": "node --test --test-reporter=dot",
        "test:watch": "node --watch --test --test-reporter=dot"
    },
    "devDependencies": {
        "@types/node": "^24.10.1"
    }
}

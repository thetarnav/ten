{
    "name": "lilang",
    "type": "module",
    "version": "0.0.0",
    "scripts": {
        "test": "node --experimental-transform-types --test",
        "test:watch": "node --watch --experimental-transform-types --test"
    },
    "devDependencies": {
        "@types/node": "^24.10.1"
    }
}
